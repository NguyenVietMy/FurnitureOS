# FurnitureOS landing page implementation plan

> Historical landing delivery record. The Next.js runtime and `web/` startup instructions below are superseded by the root Vite app. See [the frontend guide](../frontend.md) and [current integration evidence](verification/integration.md).

## Goal and authorization

Build a runnable FurnitureOS landing page closely matching https://superwall.com/ as observed September 8, 2026, plus a root DESIGN.md governing future UI. User authorized implementation and a claudex loop with Sol replacing Claude. Coordinator owns planning/arbitration and proof; native Sol sessions review/build; a fresh non-builder session inspects. Native agent transport replaces the cross-provider CLI runner because the requested replacement is Sol. Maximum five plan rounds, two fix rounds, two inspection rounds. No commit, push, or publication requested.

## Baseline and isolation

Build/review checkout: C:/Users/VietM/AppData/Local/Temp/furnitureos-landing-superwall, clean detached worktree at 901265fbe84abbd0eecdeafd152f4459cdb5decc. Delivery checkout: C:/Users/VietM/OneDrive/Documents/VietMySpace/FurnitureOS. Preserve original modifications .claude/skills/astra-loop/SKILL.md and untracked .agents/ and GUIDE.md. Work only on web/, root DESIGN.md, a short UI pointer appended to AGENTS.md, root .gitignore, and docs/landing/. Plan hash is bound to review checkout and approved file. Coordinator will copy authorized additions/AGENTS pointer to delivery checkout after inspection without changing existing work.

## Assumptions and sources

- CONTEXT.md: photos of a real Room become a furnished 3D Design assembled from actual Catalogue Products. Room Types are bedroom/living room, Style chosen visually, Swap preserves footprint.
- docs/adr/0001-0003: dimensions confirmed by user; geometry from multi-photo Capture plus shape and numeric dimensions; LLM selects and solver places. No operational application exists at baseline, so this task delivers marketing and a clearly marked interactive sample, not real generation or purchase functionality.
- User said a straight copy of the design with changed content is acceptable, with at least 90% match desired. Fidelity refers to visual grammar and comparable section compositions, not copied text or invented traction. Do not claim a measured 90% pixel score; assess against a recorded rubric and disclose content-driven differences.
- Primary action: Explore a sample Design (pending optional user steering). It opens an accessible sample dialog with Room Type/Variant controls and a deterministic demonstration Swap. No email collection/backend claims. Hero pill can use a Room Type select on left and sample CTA on right, preserving reference proportions. All repeated CTAs use this same flow.
- No product testimonials, customer logos, metrics, prices, affiliate relationships, or active app links may be invented. Reference logo wall/stat/pricing/testimonial compositions are adapted to product concepts, sample facts, workflow choices, and FAQs.

## Measured design reference

Reference screenshots are available in delivery checkout: superwall-desktop.png (1440x1000), superwall-mobile.png (390x844), superwall-full.png. Inspect with view_image before coding. Public site's main stylesheet is https://superwall.com/_astro/BaseLayout.BPT1MZF2.css. Observed light mode:

- Background #fdfef6, ink #0c0b0a, border #e2e4d0, cyan #67edec/#46d7d4, teal ink #143e3d, navy #29407c, periwinkle #446bce. Secondary orange accent can be sampled from stylesheet if required.
- Main frame 1280px at 1440 viewport, x80; continuous thin vertical side borders. Full viewport cyan announcement ~33px; sticky ivory nav ~65px desktop. Mobile nav two rows ~93px after announcement.
- Hero from y98 to630 on desktop (~532px), center aligned. Max heading width1024, Manrope Variable weight500, 88px/83.6px, tracking -4.4px; two lines. Mobile h1 48px/.95 tracking -.05em, wrapping 3-4 lines. Hero body 18px, muted ink, top gap24; pill CTA max672px, about74px high, margin top32. Hero desktop py~104, mobile generous py~104; screenshot is source of truth.
- Hero repeating grid cells10x55px, 1px borders, black20px corner brackets. Grid should remain subtly visible behind text; no gradients replacing this pattern.
- Cyan pill buttons have a fine dither/pixel pattern, dark text, smaller black rectangular nav CTA. Build lightweight CSS/SVG pattern instead of shader/WebGL.
- Four-tab row min62px, vertical dividers, active teal bottom rule; 2x2 at <=640px. Large framed product preview underneath with grid surround and ~56px desktop padding. Illustrate FurnitureOS with room/product preview content, not Superwall screenshots.
- Section spacers64px desktop. Big headings80px/1, tracking -.05em; feature headings28px/1.1. Features are bordered split rows (~40/60%), text left, visual right, not floating rounded cards. Rounded corners confined to CTAs and internal product UI.
- Preserve dark patterned CTA band, blue patterned feature band, four-column bordered comparison area, editorial multi-column content, final dark CTA, spacious footer and large wordmark motif. Reference full page has many repeated customer examples; adapt this repetition proportionately without fake endorsements.
- Manrope is open-source; install @fontsource-variable/manrope with its license. Use an open-source mono e.g. @fontsource/roboto-mono for small decorative labels instead of proprietary Quadrant. System fallback required. Self-host fonts via package imports.

## Implementation

Create web/ with Next.js App Router + React + TypeScript, respecting docs/build-plan.md phase0 frontend direction. npm lockfile, scripts dev/build/typecheck/test:e2e (Playwright), plus start. Keep tooling small; no backend or application-generation stack needed for this marketing task. Use stable current compatible versions from npm metadata; do not alter global installations. Node22.23.2 supports verified Next16.3.4. Serve root route, static prerendering with normal Next build, dev/start port3000 (must fail rather than silently change port); set a distinct build directory for dev if needed to avoid concurrent dev/build corruption. Root .gitignore excludes web/node_modules, web/.next, web/.next-dev, tsbuildinfo, test results, playwright report and .playwright-mcp; preserve existing entries if any. Use next/image for local imagery and next/font/local pointing at licensed npm font assets (no Google network dependency in build). Server layout/page for metadata and structure; client boundary for interactive page controls.

Deliver full responsive one-page experience: announcement, nav with working anchors/mobile menu, two-line centered hero, pill control, four product tabs, large room preview, four truthful concept stats, Catalogue/category tile wall in reference's bordered grid, dark CTA, six split feature sections (Capture, Room Shell, Variants, Swap, real Products, Style), blue patterned band, workflow comparison columns, sample Design gallery, FAQ, final CTA and footer. Copy concise and product specific. All visualized sample rooms and products explicitly illustrative; no fabricated buy links or prices. Use attractive furnished-room imagery for story and a diagram/product schematic for fit; avoid pretending stock images are generated by FurnitureOS. Images must be local, provenance recorded in docs/landing/assets.md. Can use openly accessible Unsplash images downloaded from images.unsplash.com with appropriate source/credit, or original generated imagery if available. Prefer 2-4 quality local images reused carefully rather than many network requests. If unavailable, make a deliberate original SVG room illustration instead. Decorative SVG patterns/icons can be authored directly.

Interactions: product tabs update matching panel/description; arrow/Home/End keyboard tab navigation, aria-selected/tabindex controls; sample dialog native dialog with close/Escape and restored trigger focus; Room Type and Variant change displayed sample; Swap toggles one illustrated Product and visibly updates label; mobile navigation closes on link selection and Escape; FAQ native details. No fake submit forms, placeholder hrefs, dead controls, or hidden active panels in keyboard order. Respect prefers-reduced-motion, focus-visible, contrast, meaningful image alt, single h1 and landmark labels. No auto-rotating tabs needed.

Sample truthfulness contract: describe intended real-Product capability as FurnitureOS's product direction, not a shipped Catalogue or purchasing claim. In the running preview, call fixtures illustrative furnishings/sample concepts, with an always-visible 'Interactive concept preview. Illustrative furnishings; no live Catalogue or room generation.' disclosure. Photo images are Style inspiration, never generated Design output. The Swap demo changes a separate schematic furnishing with explicit equal footprint fixtures (e.g. chair A/B both0.8x0.8m), identical x/y/rotation, and only different material/silhouette and label. It demonstrates the rule, not solver validation or actual Product availability. Variant controls change a separate schematic arrangement within the same Room Shell, not unrelated room photos. Add behavioral assertions for footprint and Placement invariance and review copy for false availability, generation, or purchase claims.

DESIGN.md is concise authoritative guidance: provenance, exact CSS token names/values aligned with implementation, typography and breakpoints, frame/grid and section rules, component recipes, content/domain/illustration rules, motion/accessibility, and fidelity validation checklist. Root AGENTS.md gains one short pointer requiring reading DESIGN.md before UI additions/changes/reviews. Do not duplicate every implementation detail in documentation. Also include README/run instructions in web and short reference/rubric doc under docs/landing.

## Acceptance and proof

- npm --prefix web install (first setup); npm --prefix web run typecheck; npm --prefix web run build; npm --prefix web run test:e2e must pass.
- E2E should cover real interactions (sample open/close/focus/Swap/Variant; keyboard tabs; mobile menu; all anchor targets; viewport overflow at390,768,1440; reduced motion where relevant), not mirror static implementation. No real network dependency in app at runtime for fonts/images.
- Coordinator independently runs proof and uses Playwright screenshots at1440x1000 and390x844 plus full page to compare against reference. Inspect 768px and keyboard/console too. Evidence screenshots kept under docs/landing/reference and docs/landing/verification (or ignored diagnostics if large); note actual tested widths.
- Fidelity rubric: use the fixed checklist below, each item worth5 points, pass/fail,20 checks total. Record measurements and screenshot links per item; required score>=90/100. This is a transparent design-conformance score, not a pixel-similarity percentage; never present it as the latter. Explain brand/copy/product-image, truthful-proof and mono-font differences separately. Frame+geometry20: (1)1440 frame width1280±2 and x80±2; (2)desktop announcement33±3/nav65±4; (3)desktop hero height532±32 and centered heading maxwidth1024±8; (4)major section spacer64±4 and feature split5fr/7fr±2percentage points with32±4/48±4 horizontal padding. Typography20: (5)loaded Manrope weight500 hero; (6)desktop hero88±1 lineheight83.6±2 tracking-4.4±.2; (7)large section titles80±2/lineheight80±4, feature titles28±1/30.8±2; (8)mobile hero48±1/.95±.03 and body16-18px. Palette+patterns20: (9)exact background/ink/border #fdfef6/#0c0b0a/#e2e4d0; (10)exact cyan/navy accents #67edec/#46d7d4/#29407c; (11)hero grid10x55±1 with1px lines; (12)20±2px black hero corner brackets and visible dither button texture. Component styling15: (13)hero pill max672±8 and height74±8 with cyan right CTA; (14)desktop four equal tabs62±4 high with dividers and teal selected rule; (15)square black nav CTA, thin section borders and no rounded floating marketing cards. Section composition15: (16)large framed preview with grid and desktop surround56±8; (17)dark CTA plus blue patterned band plus four-column bordered comparison; (18)six5:7 feature rows, editorial gallery/FAQ, spacious footer/wordmark present. Responsive10: (19)390px two-row nav and2x2 tabs, one-column features; (20)no horizontal overflow at390,768,1440, readable controls and preserved hierarchy. Content-driven text wrapping is allowed within hero-height tolerance. Reference full-page repetition is compressed to truthful useful content; disclose that whole-page length is intentionally different.
- Independent plan reviewer must return APPROVED/REVISE/BLOCKED with findings evidence, coverage/limits and exact plan SHA256. Fresh final inspector examines all added files and docs, actual screenshot evidence and coordinator proof, returning findings and limitations. No later code changes can inherit earlier approval.

## Delivery integrity

Transfer only web/ source, public assets, package/lock/config/readme/test files; root DESIGN.md/.gitignore; the reviewed AGENTS.md change; docs/landing/. Exclude node_modules, .next/.next-dev, test-results, generated Next env types if necessary and unrelated repository files. Verify original AGENTS.md remains at the recorded baseline before applying its change. Generate a sorted path+SHA256 manifest of every allowlisted delivered file in both checkouts and require equality; inspect final delivery git diff/untracked manifest for scope and preserve pre-existing changes. Run npm --prefix web ci, typecheck, build and test:e2e from delivery checkout. Use a separate test server port or stop the identified worktree server to avoid accidentally testing the other checkout. Any source mismatch requires fresh inspection; byte-identical transfer inherits the inspected snapshot with equality evidence. Store final proof and manifest evidence in docs/landing/verification; evidence-only log additions do not alter the source snapshot.
