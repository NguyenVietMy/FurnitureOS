# Coding standards (durable)

Mirror of `.claude/skills/coding-standards`. These are PUSHED into the review step
so the reviewer compares code against them directly.

- Deep modules, small interfaces.
- TDD always; test at the module boundary.
- Explicit types at boundaries; no swallowed errors.
- Must pass lint/format and type-check.
- <add project-specific rules>
